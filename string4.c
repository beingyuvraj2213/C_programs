#include <stdio.h>
#include <string.h>
int main()
{
    // char str4[100];
    // char str5[100];
    // char str1[50];
    // printf("Enter the first name : ");
    // gets(str1);
    // char str2[50];
    // printf("\nEnter the second name : ");
    // gets(str2);
    char str3[] = " is a friend of ";
    printf("%s",str3);
    // strcpy(str4, strcat(str1, str3));
    // strcpy(str5, strcat(str4, str2));
    

    // puts(str5);
    return 0;
}