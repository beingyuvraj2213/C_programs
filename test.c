#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int yuvraj(int x)
{
        if(x>3)
       return 0;
       else
       return 1;
}
int main() {
       int m=2;
       int r=yuvraj(m);
       printf("%d",r);
   
}